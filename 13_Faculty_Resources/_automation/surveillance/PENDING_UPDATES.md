# Surveillance — Pending Update Proposals

Auto-appended by `bin/open_update_pr.py` when a monitored guideline source changes a page we teach.
Each entry corresponds to an open PR (branch `surveillance/update-<fp8>`) carrying the source diff, the
affected page(s), and an attestation checklist. An entry clears when its PR is merged or closed.
