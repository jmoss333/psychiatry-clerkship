name: CI — build, test, validate

# Fast pre-merge gate on every PR: unit test + topic_meta contract + no hard-coded
# machine paths + full site build & static QA harness. Mirrors what Netlify runs, so
# breakage turns a PR red instead of only surfacing at deploy time.
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * 0"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'schedule' }}

jobs:
  build-test-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          lfs: false
          fetch-depth: 0

      - name: Agent docs parity — AGENTS.md must equal CLAUDE.md
        run: |
          if ! cmp -s CLAUDE.md AGENTS.md; then
            echo "::error::AGENTS.md is out of sync with CLAUDE.md — run: cp CLAUDE.md AGENTS.md"
            exit 1
          fi
          echo "agent docs parity OK"

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.11"

      - name: Install — registry schema validation dependencies
        run: python3 -m pip install --requirement requirements.txt PyYAML==6.0.2

      - name: Unit — scheduled maintenance
        run: python3 -m unittest discover -s tests/maintenance -p 'test_*.py' -v

      - name: Validate — scheduled workflow contracts
        run: python3 13_Faculty_Resources/_automation/maintenance/validate_scheduled_workflows.py

      - name: Lint — no hard-coded machine paths in tracked Python
        run: |
          if git grep -nE "/(Users|sessions)/[a-z]" -- "*.py"; then
            echo "::error::hard-coded /Users or /sessions path in tracked .py — derive from __file__"; exit 1
          fi
          echo "path lint OK"

      - name: Unit — media guard
        run: python3 13_Faculty_Resources/_automation/site_build/test_media_guard.py

      - name: Unit — shared build logic (common.py)
        run: python3 13_Faculty_Resources/_automation/site_build/test_common.py

      - name: Unit — evidence registry
        run: python3 tools/evidence_registry/test_registry.py

      - name: Validate — evidence registry and generated views
        run: python3 tools/evidence_registry/validate.py --check-generated

      - name: Unit — citation surveillance
        run: python3 13_Faculty_Resources/_automation/surveillance/bin/run_citation_check.py --self-test

      - name: Unit — resource intake sanitization
        run: python3 13_Faculty_Resources/_automation/surveillance/bin/run_resource_intake.py --self-test

      - name: Validate — topic_meta.json contract
        run: python3 13_Faculty_Resources/_automation/validate_topic_meta.py

      - name: Test — claim-anchor validator
        run: python3 13_Faculty_Resources/_automation/test_validate_claim_anchors.py

      - name: Validate — claim anchors resolve to declared evidence
        run: python3 13_Faculty_Resources/_automation/validate_claim_anchors.py

      - name: Test — evidence drill generator
        run: python3 13_Faculty_Resources/_automation/test_generate_evidence_drill.py

      - name: Validate — evidence drill is regenerated from current anchors
        run: python3 13_Faculty_Resources/_automation/generate_evidence_drill.py --check

      - name: Validate — longitudinal case contract
        run: python3 13_Faculty_Resources/_automation/test_longitudinal_case.py

      - name: Unit — shelf/COMAT question bank data-quality gate
        run: python3 09_Exam_Prep/shelf_comat_bank/engine/test_qbank.py

      - name: Validate — family systems scenarios contract
        run: python3 13_Faculty_Resources/_automation/test_family_systems_scenarios.py

      - name: Test — registry schema gate
        run: python3 13_Faculty_Resources/_automation/test_validate_registry_schemas.py

      - name: Validate — registry schemas
        run: python3 13_Faculty_Resources/_automation/validate_registry_schemas.py

      - name: Test — rotation edition schema gate
        run: python3 13_Faculty_Resources/_automation/test_validate_rotation_edition_schema.py

      - name: Validate — rotation edition schema fixtures
        run: python3 13_Faculty_Resources/_automation/validate_rotation_edition_schema.py

      - name: Test — rotation edition catalog gate
        run: python3 13_Faculty_Resources/_automation/test_validate_rotation_edition_catalog.py

      - name: Validate — immutable rotation edition catalog
        env:
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
        run: |
          if [[ "$BASE_SHA" =~ ^[0-9a-f]{40}$ && "$BASE_SHA" != "0000000000000000000000000000000000000000" ]]; then
            python3 13_Faculty_Resources/_automation/validate_rotation_edition_catalog.py --compare-ref "$BASE_SHA"
          else
            python3 13_Faculty_Resources/_automation/validate_rotation_edition_catalog.py
          fi

      - name: Guard — production rotation edition stays exactly empty and disabled
        run: |
          python3 - <<'PY'
          from pathlib import Path

          root = Path("13_Faculty_Resources/Rotation_Curation")
          expected = {
              "rotation_edition_catalog.json": b'{"schemaVersion":1,"records":[]}\n',
              "rotation_edition_catalog_governance.json": b'{"schemaVersion":1,"manifestRevision":1,"rotationEditionV2":"disabled","dispositions":[]}\n',
          }
          for name, locked in expected.items():
              observed = (root / name).read_bytes()
              if observed != locked:
                  raise SystemExit(f"{name} must remain the exact empty/disabled production lock")
          PY

      - name: Test — ReConnect snapshot provenance
        run: python3 13_Faculty_Resources/_automation/test_validate_reconnect_snapshot_provenance.py

      - name: Validate — ReConnect snapshot provenance
        run: python3 13_Faculty_Resources/_automation/validate_reconnect_snapshot_provenance.py

      - name: Unit — surface governance
        run: python3 13_Faculty_Resources/_automation/test_surface_governance.py

      - name: Unit — tool governance
        run: python3 13_Faculty_Resources/_automation/test_validate_tool_governance.py

      - name: Validate — tool governance
        run: python3 13_Faculty_Resources/_automation/validate_tool_governance.py

      - name: Install — faculty export tool dependencies
        run: python3 -m pip install --requirement requirements-dev.txt

      - name: Unit — faculty export tools (adobe packet, faculty polish, pdf library)
        run: |
          python3 tools/adobe_packet_export/test_export_ms3_adobe_packet_data.py
          python3 tools/faculty_polish_export/test_export_top10_faculty_polish.py
          python3 tools/pdf_library_export/test_export_website_pdf_library.py

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "20"

      - name: Unit — root node regression tests (tests/*.test.mjs)
        run: node --test tests/*.test.mjs

      - name: Validate — WCAG AA contrast tokens
        run: node tests/contrast-check.mjs

      - name: Install — managed SP proxy dependencies
        run: npm --prefix sp-proxy ci

      - name: Test — SP Interview and managed proxy
        run: |
          npm --prefix sp-proxy test
          bash _prototypes/sp-interview/tests/run-all.sh
          python3 13_Faculty_Resources/_automation/test_validate_attestation_consistency.py

      - name: Build + static QA gate (ms3)
        run: bash 13_Faculty_Resources/_automation/site_build/build_and_check.sh ms3

      - name: Build + static QA gate (res)
        run: bash 13_Faculty_Resources/_automation/site_build/build_and_check.sh res

  smoke-tests:
    name: Smoke tests (nav crawl · faculty console · LFS · visual)
    runs-on: ubuntu-latest
    needs: build-test-validate
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          lfs: false
          fetch-depth: 0

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.11"

      - name: Install — registry schema validation dependencies
        run: python3 -m pip install --requirement requirements.txt

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: "20"

      - name: Build sites (ms3 + res)
        run: |
          bash 13_Faculty_Resources/_automation/site_build/build_and_check.sh ms3
          bash 13_Faculty_Resources/_automation/site_build/build_and_check.sh res

      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6
        with:
          path: ~/.cache/ms-playwright
          # Key derives from the smoke lockfile so a Playwright bump rolls the
          # cache automatically (a hand-pinned version went permanently stale
          # after the 1.46.1→1.61.1 bump). Keep in sync with refresh-baselines.yml.
          key: playwright-${{ hashFiles('tests/smoke/package-lock.json') }}-${{ runner.os }}

      - name: Install Playwright + Chromium
        run: |
          cd tests/smoke
          npm ci
          npx playwright install chromium --with-deps

      - name: Start local review servers
        run: bash tests/smoke/start-local-servers.sh

      - name: "Check 1: nav crawl + two-audience rotation edition journeys — ms3 + res"
        run: |
          cd tests/smoke
          npx playwright test --project=nav-ms3 --project=nav-res

      - name: "Check 1a: Interview Room acceptance — ms3"
        run: |
          cd tests/smoke
          npx playwright test --project=interview-room
        env:
          SP_INTERVIEW_BASE_URL: http://localhost:4200/tools/

      - name: "Check 1b: Unified faculty attestation workspace"
        run: |
          cd tests/smoke
          npx playwright test --project=faculty-console

      - name: "Check 2: LFS integrity — Netlify deploy preview"
        run: |
          cd tests/smoke
          # Derive the deploy-preview URL from the PR number (if this is a PR run).
          # Netlify deploy previews use the pattern:
          #   https://deploy-preview-{PR}--{site-slug}.netlify.app
          # Probe first; skip gracefully if the deploy isn't live yet.
          PR_NUM="${{ github.event.pull_request.number }}"
          if [ "${{ github.event_name }}" = "pull_request" ] && [ -n "$PR_NUM" ]; then
            MS3_CANDIDATE="https://deploy-preview-${PR_NUM}--une-ms3-psychiatry.netlify.app"
            RES_CANDIDATE="https://deploy-preview-${PR_NUM}--mmc-psychiatry-residents-sanford.netlify.app"
            if curl -sf --head --max-time 8 "$MS3_CANDIDATE" >/dev/null 2>&1; then
              export MS3_DEPLOY_URL="$MS3_CANDIDATE"
              export RES_DEPLOY_URL="$RES_CANDIDATE"
              echo "Deploy preview ready — LFS check will run against $MS3_CANDIDATE"
            else
              echo "Deploy preview not yet live — LFS check will be skipped (set MS3_DEPLOY_URL manually to force)"
            fi
          else
            export MS3_DEPLOY_URL="https://une-ms3-psychiatry.netlify.app"
            export RES_DEPLOY_URL="https://mmc-psychiatry-residents-sanford.netlify.app"
            echo "Scheduled/manual release rehearsal — LFS check will run against production learner sites"
          fi
          npx playwright test --project=lfs
        env:
          # Override by setting repository variables MS3_DEPLOY_URL / RES_DEPLOY_URL
          MS3_DEPLOY_URL: ${{ vars.MS3_DEPLOY_URL }}
          RES_DEPLOY_URL: ${{ vars.RES_DEPLOY_URL }}

      - name: "Check 3: visual regression — resident site"
        run: |
          cd tests/smoke
          npx playwright test --project=visual

      - name: "Check 4: offline shell — service worker"
        run: |
          cd tests/smoke
          npx playwright test --project=offline

      - name: Upload smoke test artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: always()
        with:
          name: smoke-test-results-${{ github.run_number }}
          path: |
            tests/smoke/test-results/
          retention-days: 14
