# Codex handoff — Practice a Moment

Copy the prompt below into a new Codex task with access to Psychiatry-Clerkship-Library. Include the four-file packet if that task cannot read this worktree. This is a task handoff, not a request to start another task automatically.

```text
Implement Practice a Moment inside the existing protected Interview Room.

Read these documents in order; they are the execution contract:
1. docs/superpowers/specs/2026-09-08-practice-a-moment-design.md
2. docs/superpowers/specs/2026-09-08-practice-a-moment-cases.md
3. docs/superpowers/plans/2026-09-08-practice-a-moment.md

The packet currently resides in:
/Users/jm/Psychiatry-Clerkship-Library/.worktrees/sp-live-integration/
Copy only these documents and this handoff into your isolated current-main worktree if absent. Do not copy credentials, dependencies or build output.

Complete BOTH stages: Elena rupture repair and Priya patient-corrected formulation, then Luis teach-back and opt-in transfer links. Include private reflection, optional explicitly submitted team formulation, evidence-linked review, a safe static fallback and one spoken alternative. Three draft moments are the selected scope; the other ideas are intentionally excluded, not unfinished tasks.

Start from fresh origin/main containing merge 71f6ac8a8bb74f6e544255d3f3068d1c305c4086 (PR582, family/MI/portrayal). Verify current repository instructions, state, overlap and source before editing. The root checkout had unrelated CI/maintenance edits when this was written; preserve them. Use a codex/ branch and isolated worktree. Do not reset another checkout.

Use one integrating agent for shared state/controller/provider changes. Delegate pure content/projection and evidence tests where independent. Obtain an independent protocol/budget review and a separate educational-feedback challenge review before completion. Follow the writing plan task by task, with focused failing/passing tests and reviewable commits. Do not spend the task re-brainstorming the approved product direction or stop after the first milestone.

Keep the existing five full encounters, accepted voices, automatic microphone timing, password/server key configuration, and 20 starts/680 daily units/340 rolling units unchanged. Do not migrate/reset the budget ledger, add persistent transcripts or audio, log dialogue, introduce analytics, expose hidden facts, weaken CSP, or alter faculty attestation. The current private attending presentation stays private. The new team formulation is a separate explicit AI-feedback submission. No learner trait, competence, fluency or speech-rate grading.

The server enforces at most 4 patient-facing turns, one 3-unit review and one 3-unit terminal alternative. Maximum 19 reserved units and one start per moment. A transfer is a fresh normally budgeted start. No API calls occur before validation/reservation. Failed/uncertain requests are never automatically repeated. Review closes the current continuation nonce before evaluating, so old receipts cannot branch. Do not treat one-unit review as safe: it would count as an encounter start in the existing ledger.

Complete all unpaid checks and build a working staging preview plus a draft PR. Reuse existing authorized server-side configuration privately; do not ask for secrets in chat or create a new key. Paid audition is opt-in and bounded to the plan's three starts and 51 reserved units; if the task does not inherit authorization for paid provider use, prepare everything first and identify the exact bounded test for approval. Do not bypass exhausted capacity with another namespace.

Enable the new non-secret DANA_MOMENTS_ENABLED flag in the staging context so the preview actually works; production flag enablement belongs to the authorized release step. Use the existing protected site's staging target only; never deploy this package to either learner site or sp-interview-proxy. Production publication follows existing explicit owner authorization if present; otherwise deliver the concrete preview, PR and evidence before requesting that final release decision. Do not assume implementation permission confers faculty clinical approval. Keep all new cases labeled faculty-review drafts.

Finish with a requirement-by-requirement R01–R15 audit. Report exact commits, tests, browser evidence, real-provider versus synthetic checks, physical-microphone status, actual costs only if supported, PR/deploy state, and remaining faculty decisions. In plain language, explain what the student can now do, the next best check and one future idea. Do not call a quoted-span validator proof that AI feedback is clinically correct.
```

## Included packet

| File | Purpose |
|---|---|
| `docs/superpowers/specs/2026-09-08-practice-a-moment-design.md` | Scope decisions, UX, exact protocol, data boundaries, failure behavior, budget and evidence contract. |
| `docs/superpowers/specs/2026-09-08-practice-a-moment-cases.md` | Three complete fictional cases, actor directions, allowed endings, rubric/template IDs and 39 challenge cases. |
| `docs/superpowers/plans/2026-09-08-practice-a-moment.md` | File map, interfaces, ordered implementation tasks, code/test examples, gates and R01–R15 coverage. |
| This file | Paste-ready task prompt and packet index. |

## Recommended execution choice

Use Codex with its configured capable model for the integrating agent. A less expensive model can encode the already authored cards, public projection, static UI copy and straightforward fixtures under review. Keep state transitions, concurrency, heard-evidence handling and evaluator validation with the integrating agent and independent reviewer. No particular new model/API purchase is required.

## Planning completion record

This handoff is a planning artifact. No Practice a Moment runtime, new paid test, environment change, PR or deployment was performed in preparing it. The previous family/MI PR merge was verified read-only. The plan explicitly distinguishes implementation evidence from clinical/faculty review and selects a finite three-moment scope rather than all ten proposed feature families.
