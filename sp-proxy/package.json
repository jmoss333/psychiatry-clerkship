{
  "name": "sp-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Netlify function proxy for The Interview Room (LLM standardized patient). Key stays server-side.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
